version: 2

models:
  - name: stg_example
    description: >
      A placeholder staging model. Replace with your actual source tables.
    columns:
      - name: id
        description: Primary key.
        tests:
          - unique
          - not_null
